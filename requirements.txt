# Core data stack — pinned to major versions that are current as of Apr 2026.
numpy>=2.0,<3.0
pandas>=2.2,<3.0
scikit-learn>=1.5,<2.0

# Visualization
matplotlib>=3.9,<4.0
seaborn>=0.13,<1.0

# Notebook / tooling
jupyter>=1.1
ipykernel>=6.29
nbformat>=5.10

# Data retrieval (Google Drive direct download for the dataset)
gdown>=5.2

# Developer QoL (optional but recommended)
ruff>=0.6
